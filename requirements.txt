# Core runtime dependencies
pandas>=2.0,<3
numpy>=1.24,<3
scipy>=1.11,<2
pyarrow>=14.0,<20
requests>=2.28,<3
scikit-learn>=1.3,<2
python-dateutil>=2.8

# Production gradient-boosting backend used by daily training.
# CI installs the CPU-only wheel explicitly to avoid optional accelerator deps.
