# Core runtime dependencies
pandas>=2.0,<3
numpy>=1.24,<3
pyarrow>=14.0,<20
requests>=2.28,<3
scikit-learn>=1.3,<2
python-dateutil>=2.8

# Production gradient-boosting backends
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# CPU-only neural-network backends for GitHub Actions.
# The runners do not need CUDA; unqualified torch pulls hundreds of MB of
# NVIDIA CUDA libraries and can exhaust the runner disk before installation.
torch==2.8.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu
tensorflow-cpu>=2.15,<2.20
