# Core runtime dependencies
pandas>=2.0,<3
numpy>=1.24,<3
pyarrow>=14.0,<20
requests>=2.28,<3
scikit-learn>=1.3,<2
python-dateutil>=2.8

# Production gradient-boosting backends
# CI installs the CPU-only XGBoost wheel separately because generic XGBoost
# can pull a large NCCL package on CPU-only runners.
lightgbm>=4.0
catboost>=1.2

# Neural-network backend used by the simulator/trainer.
# CI installs CPU-only PyTorch explicitly from the official wheel index.
tensorflow-cpu>=2.15,<2.20
