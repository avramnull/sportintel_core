# Core — required for scraper, parquet, sim, publish
pandas>=2.0,<3
numpy>=1.24,<3
pyarrow>=14.0,<20
requests>=2.28,<3
scikit-learn>=1.3,<2
python-dateutil>=2.8

# Boosters — install in CI as hard deps; local optional
 xgboost>=2.0
 lightgbm>=4.0
 catboost>=1.2

 Neural nets — optional
 torch
 tensorflow>=2.15
