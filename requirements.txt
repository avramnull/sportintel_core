# Core runtime dependencies
pandas>=2.0,<3
numpy>=1.24,<3
pyarrow>=14.0,<20
requests>=2.28,<3
scikit-learn>=1.3,<2
python-dateutil>=2.8

# Production gradient-boosting backends
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# Neural-network backends used by the simulator/trainer.
# PyTorch is installed explicitly from the official CPU wheel index by CI;
# keeping it out of this requirements file prevents a default PyPI resolver
# from selecting the CUDA build and exhausting GitHub-hosted runner storage.
tensorflow-cpu>=2.15,<2.20
